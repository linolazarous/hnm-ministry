{
  "scripts": {
    "build": "vite build",
    "preinstall": "npm install vite --save-dev"  # Safety net for Vite installation
  },
  "devDependencies": {
    "vite": "^5.0.0",               # Confirm version matches your project
    "@vitejs/plugin-react": "^4.2.1" # Required for React projects
  }
}
